# Dependencies
Copy and paste this code into the `build.dependencies.gradle` file's `dependencies` list where it
says `implementation` over and over again.

```gradle
implementation 'com.acmerobotics.dashboard:dashboard:0.4.16'
implementation 'com.fasterxml.jackson.core:jackson-databind:2.13.4.2'
```