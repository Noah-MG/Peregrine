package org.firstinspires.ftc.teamcode.peregrine.core.utilities;

/** Shared enum types for the library. */
public class Enums {

    /** Drivetrain type, selected in RobotParams.chassis. Only MECANUM is implemented in Kinematics. */
    public enum Chassis {
        MECANUM,
        SWERVE,
        DIFFYSWERVE,
        BALL
    }

}
